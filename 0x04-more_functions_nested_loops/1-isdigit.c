#include "main.h"


/**
 * _isdigit - checks if character is a digit
 *@c: character to be checked

 * Return: 1 if @c is a digit
 * otherwise  0.
 */

int _isdigit(int c)
{
    char c;

    if (c >= 48 && c <= 57)
    {
            _putchar('1');
    }
    else
    {
            _putchar('0');

    return (0);
}
